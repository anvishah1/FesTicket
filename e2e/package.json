{
  "name": "tiqr-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end Playwright test suite for tiqr (drives frontend + backend together)",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
