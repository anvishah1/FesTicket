node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# never commit local databases (the app uses Postgres; dev.db held plaintext creds)
prisma/*.db
*.db

# test coverage
/coverage
